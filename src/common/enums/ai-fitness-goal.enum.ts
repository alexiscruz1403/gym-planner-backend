export enum AiFitnessGoal {
  MUSCLE_GAIN = 'muscle_gain',
  FAT_LOSS = 'fat_loss',
  BODY_RECOMPOSITION = 'body_recomposition',
  STRENGTH = 'strength',
  ENDURANCE = 'endurance',
  GENERAL_HEALTH = 'general_health',
  MOBILITY = 'mobility',
}
